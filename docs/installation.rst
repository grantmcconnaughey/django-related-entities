============
Installation
============

At the command line::

    $ easy_install django-related-entities

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-related-entities
    $ pip install django-related-entities
